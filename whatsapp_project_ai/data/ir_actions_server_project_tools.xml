<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_actions_server_ai_create_project" model="ir.actions.server">
        <field name="name">AI Project: Create Project</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="True"/>
        <field name="code">ai['result'] = record.sudo()._ai_create_project(name, description, user_id, partner_id, tag_ids, stage_names)</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="ai_tool_description">## Tool: Create a Project
Use when the user has confirmed the work and you should open a project.</field>
        <field name="ai_tool_schema">
        {
            "properties": {
                "name": {"type": "string", "description": "Project name"},
                "description": {"type": "string", "description": "Project description"},
                "user_id": {"type": "number", "description": "Assigned user id"},
                "partner_id": {"type": "number", "description": "Customer partner id"},
                "tag_ids": {"type": "array", "items": {"type": "number"}, "description": "Project tag ids"}
                ,
                "stage_names": {"type": "array", "items": {"type": "string"}, "description": "Task stage names for this project (optional). If omitted, defaults are Todo, Doing, Review, Done."}
            },
            "required": ["name", "description"],
            "type": "object"
        }
        </field>
    </record>

    <record id="ir_actions_server_ai_get_project_create_available_params" model="ir.actions.server">
        <field name="name">AI Project: Get Project creation parameters</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="code">ai['result'] = record.sudo()._ai_get_project_create_available_params()</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="ai_tool_description">## Tool: Get Project Creation Parameters
Retrieves available users and tags for project creation.
Use this tool only immediately before calling the project creation tool.</field>
        <field name="ai_tool_schema">
        {
            "properties": {},
            "required": [],
            "type": "object"
        }
        </field>
    </record>

    <record id="ir_actions_server_ai_follow_up_project" model="ir.actions.server">
        <field name="name">AI Project: Follow Up Project</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="False"/>
        <field name="code">ai['result'] = record.sudo()._ai_follow_up_project()</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="ai_tool_description">## Tool: Follow up on a Project
Return the current status for the latest projects and their recent tasks.</field>
        <field name="ai_tool_schema">
        {
            "properties": {},
            "required": [],
            "type": "object"
        }
        </field>
    </record>

    <record id="ir_actions_server_ai_create_task" model="ir.actions.server">
        <field name="name">AI Project: Create Task</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="True"/>
        <field name="code">ai['result'] = record.sudo()._ai_create_task(name, description, project_id, user_id, tag_ids, deadline_days, stage_names)</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="ai_tool_description">## Tool: Create a Task
Use for support, fixes, or revisions inside an existing project.</field>
        <field name="ai_tool_schema">
        {
            "properties": {
                "name": {"type": "string", "description": "Task name"},
                "description": {"type": "string", "description": "Task description"},
                "project_id": {"type": "number", "description": "Target project id"},
                "user_id": {"type": "number", "description": "Assigned user id"},
                "tag_ids": {"type": "array", "items": {"type": "number"}, "description": "Task tag ids"},
                "deadline_days": {"type": "number", "description": "Deadline in days"},
                "stage_names": {"type": "array", "items": {"type": "string"}, "description": "Task stage names to ensure on the project (optional). If omitted, defaults are Todo, Doing, Review, Done."}
            },
            "required": ["name", "description", "project_id"],
            "type": "object"
        }
        </field>
    </record>

    <record id="ir_actions_server_ai_get_task_create_available_params" model="ir.actions.server">
        <field name="name">AI Project: Get Task creation parameters</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="code">ai['result'] = record.sudo()._ai_get_task_create_available_params()</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="ai_tool_description">## Tool: Get Task Creation Parameters
Retrieves available projects, users and tags for task creation.
Use this tool only immediately before calling the task creation tool.</field>
        <field name="ai_tool_schema">
        {
            "properties": {},
            "required": [],
            "type": "object"
        }
        </field>
    </record>

    <record id="ir_actions_server_ai_follow_up_task" model="ir.actions.server">
        <field name="name">AI Project: Follow Up Task</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="False"/>
        <field name="code">ai['result'] = env['whatsapp.project.ai.orchestrator'].action_follow_up_task(record, task_name, project_name)</field>
        <field name="model_id" ref="whatsapp_evaluation.model_whatsapp_evaluation_message"/>
        <field name="ai_tool_description">## Tool: Follow up on a Task
Return the current status for the latest tasks linked to the contact.</field>
        <field name="ai_tool_schema">
        {
            "properties": {
                "task_name": {"type": "string", "description": "Optional task name filter"},
                "project_name": {"type": "string", "description": "Optional project name filter"}
            },
            "required": [],
            "type": "object"
        }
        </field>
    </record>
</odoo>
