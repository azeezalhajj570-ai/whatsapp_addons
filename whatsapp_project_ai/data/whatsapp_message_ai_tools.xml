<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_actions_server_ai_msg_create_project" model="ir.actions.server">
        <field name="name">AI WhatsApp: Create Project</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="False"/>
        <field name="model_id" ref="whatsapp_evaluation.model_whatsapp_evaluation_message"/>
        <field name="code">
try:
    service_id
except NameError:
    service_id = False
try:
    rationale
except NameError:
    rationale = False
try:
    stage_names
except NameError:
    stage_names = False
try:
    name
except NameError:
    name = False
try:
    description
except NameError:
    description = False

product = env['product.template'].sudo().browse(service_id) if service_id else None
project_title = name or rationale
project_desc = description or record.body or ""
ai_data = {'rationale': project_title, 'stage_names': stage_names}
project = env['whatsapp.project.ai.orchestrator'].sudo().action_create_project(record.sudo(), product=product, ai_data=ai_data)
if project_desc and not project.description:
    project.sudo().write({'description': project_desc})
msg = f"Project '{project.display_name}' has been created successfully."
env['whatsapp.project.ai.orchestrator'].sudo().action_reply_to_user(record.sudo(), msg)
ai['result'] = msg
        </field>
        <field name="ai_tool_description">Create a project from the inbound WhatsApp conversation.</field>
        <field name="ai_tool_schema">
        {
            "properties": {
                "service_id": {"type": "number", "description": "Optional service (product.template) id"},
                "name": {"type": "string", "description": "Project name"},
                "description": {"type": "string", "description": "Project description"},
                "rationale": {"type": "string", "description": "Fallback project title/reason from user input"},
                "stage_names": {"type": "array", "items": {"type": "string"}, "description": "Optional task stage names"}
            },
            "required": [],
            "type": "object"
        }
        </field>
    </record>

    <record id="ir_actions_server_ai_msg_create_task" model="ir.actions.server">
        <field name="name">AI WhatsApp: Create Task</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="False"/>
        <field name="model_id" ref="whatsapp_evaluation.model_whatsapp_evaluation_message"/>
        <field name="code">
try:
    service_id
except NameError:
    service_id = False
try:
    rationale
except NameError:
    rationale = False
try:
    deadline_days
except NameError:
    deadline_days = False
try:
    stage_names
except NameError:
    stage_names = False
try:
    name
except NameError:
    name = False
try:
    description
except NameError:
    description = False

product = env['product.template'].sudo().browse(service_id) if service_id else None
task_title = name or rationale
task_desc = description or record.body or ""
ai_data = {'rationale': task_title, 'deadline_days': deadline_days, 'stage_names': stage_names}
task = env['whatsapp.project.ai.orchestrator'].sudo().action_create_task(record.sudo(), product=product, ai_data=ai_data)
if task_desc and not task.description:
    task.sudo().write({'description': task_desc})
project_title = task.project_id.display_name if task.project_id else "No Project"
msg = f"Task '{task.display_name}' has been created in project '{project_title}'."
env['whatsapp.project.ai.orchestrator'].sudo().action_reply_to_user(record.sudo(), msg)
ai['result'] = msg
        </field>
        <field name="ai_tool_description">Create a task from the inbound WhatsApp conversation.</field>
        <field name="ai_tool_schema">
        {
            "properties": {
                "service_id": {"type": "number", "description": "Optional service (product.template) id"},
                "name": {"type": "string", "description": "Task name"},
                "description": {"type": "string", "description": "Task description"},
                "rationale": {"type": "string", "description": "Fallback task title/content from user input"},
                "deadline_days": {"type": "number", "description": "Optional deadline in days"},
                "stage_names": {"type": "array", "items": {"type": "string"}, "description": "Optional project task stage names"}
            },
            "required": [],
            "type": "object"
        }
        </field>
    </record>

    <record id="ir_actions_server_ai_msg_follow_up_project" model="ir.actions.server">
        <field name="name">AI WhatsApp: Follow Up Project</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="False"/>
        <field name="model_id" ref="whatsapp_evaluation.model_whatsapp_evaluation_message"/>
        <field name="code">ai['result'] = env['whatsapp.project.ai.orchestrator'].sudo().action_follow_up_project(record.sudo())</field>
        <field name="ai_tool_description">Return project follow-up for the inbound contact.</field>
        <field name="ai_tool_schema">
        {"properties": {}, "required": [], "type": "object"}
        </field>
    </record>

    <record id="ir_actions_server_ai_msg_reply_to_user" model="ir.actions.server">
        <field name="name">AI WhatsApp: Reply To User</field>
        <field name="state">code</field>
        <field name="use_in_ai" eval="True"/>
        <field name="ai_tool_allow_end_message" eval="False"/>
        <field name="model_id" ref="whatsapp_evaluation.model_whatsapp_evaluation_message"/>
        <field name="code">
try:
    reply_text
except NameError:
    reply_text = False
ai['result'] = env['whatsapp.project.ai.orchestrator'].sudo().action_reply_to_user(record.sudo(), reply_text)
        </field>
        <field name="ai_tool_description">Send a direct WhatsApp reply to the user for clarifications or fallback responses.</field>
        <field name="ai_tool_schema">
        {
            "properties": {
                "reply_text": {"type": "string", "description": "Message to send to the user"}
            },
            "required": ["reply_text"],
            "type": "object"
        }
        </field>
    </record>
</odoo>
